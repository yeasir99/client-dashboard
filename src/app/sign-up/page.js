import RegestrationContainer from '@/components/registration/registrationContainer';

const page = () => {
  return (
    <div className="max-w-screen min-h-screen mx-auto bg-hero-image bg-cover flex justify-center items-center py-10">
      <RegestrationContainer />
    </div>
  );
};

export default page;
